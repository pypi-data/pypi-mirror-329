[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "uvinert"
version = "0.6.2.dev2"
description = "Custom inert build backend for UV monorepo"
dependencies = []
