[project]
name = "init-data-python"
version = "1.0.1"
description = "A Python library that offers simple interface for validating Telegram Mini App initialization data."
authors = [
    { name = "artender", email = "artem1347929@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
license = { file = "LICENSE" }

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"