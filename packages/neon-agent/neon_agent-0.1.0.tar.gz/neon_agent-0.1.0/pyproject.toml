[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neon-agent"
version = "0.1.0"
description = "systems that exhibit a higher level of autonomy and decision-making ability, without direct human intervention'"
authors = [
    {name = "Vikash Singh", email = "Vikash.Singh@Linuxmail.org"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ollama==0.4.7",
    "duckduckgo-search==6.3.5",
    "langchain-community==0.3.17",
    "langchain-community==0.3.17"
]

[project.urls]
"Homepage" = "https://github.com/VikashS/AIAgent"