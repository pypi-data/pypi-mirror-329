[project]
name = "tessie_relay"
version = "0.1.0"
description = "Relay and control for tessie"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "influxdb-client>=1.47.0",
    "paho-mqtt>=2.1.0",
]
