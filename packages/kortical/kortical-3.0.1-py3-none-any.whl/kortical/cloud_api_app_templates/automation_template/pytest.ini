[pytest]
markers =
    unit: runs tests that don't need an instantiated system.
    integration: runs tests that do require an instantiated system
    smoke: run the minimum tests to test core functionality