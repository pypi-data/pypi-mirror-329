[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transform_text"
version = "0.1.0"
description = "An example package that transforms text to lowercase"
authors = [
  { name="Blam's", email="vincentcapek@gmail.com" }
]
dependencies = [
  "stringcase>=1.2.0"
]

# Vous pouvez ajouter d'autres informations, comme la licence, la page GitHub, etc.
