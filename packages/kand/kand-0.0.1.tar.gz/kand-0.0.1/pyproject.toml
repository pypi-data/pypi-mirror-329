[project]
name = "kand"
version = "0.0.1"
description = "Python wrapper for Kand"
readme = "README.md"
authors = [
    { name = "CtrlX", email = "gitctrlx@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
