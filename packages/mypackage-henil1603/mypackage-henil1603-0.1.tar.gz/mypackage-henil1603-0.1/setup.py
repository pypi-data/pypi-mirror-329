from setuptools import setup, find_packages

setup(
    name="mypackage-henil1603",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List dependencies here, e.g., "requests"
    ],
)
