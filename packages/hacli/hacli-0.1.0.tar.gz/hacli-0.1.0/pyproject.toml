[project]
name = "hacli"
version = "0.1.0"
description = ""
authors = [
    {name = "codehamal",email = "codehamal@example.com"}
]
readme = "README.md"
requires-python = ">=3.9.0"
dependencies = [
    "typer (>=0.15.1,<0.16.0)"
]

[tool.poetry]
packages = [{include = "hacli", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"


[tool.poetry.scripts]
hacli = "hacli.main:app"