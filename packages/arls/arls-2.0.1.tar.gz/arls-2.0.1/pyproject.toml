[project]
name = "arls"
version = "2.0.1"
authors = [
  { name="Rondall E. Jones", email="rejones7@msn.com" }
]
description = "Autoregularized solver plus constraints for ill-conditioned linear systems of any shape."
readme = "README.txt"
requires-python = ">=3.8"
[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"