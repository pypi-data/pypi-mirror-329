[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flashmoe"
version = "0.0.1.dev"
description = "Sample Python package"
authors = [{ name = "Zhan Lu", email = "lausannel666@gmail.com" }]
license = { text = "MIT" }
dependencies = []