[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyeg"
version = "0.1.0"
authors = [{ name = "Your Name", email = "your.email@example.com" }]
description = "A simple pyeg parser"
readme = "README.md"
requires-python = ">=3.6"
dependencies = ["lark-parser"]
license = {file = "LICENSE"}

[tool.setuptools]
packages = ["pyeg"]
