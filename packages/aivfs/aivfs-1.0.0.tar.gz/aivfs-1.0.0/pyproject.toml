[project]
name = "aivfs"
version = "1.0.0"
description = "AI Virtual FileSystem"
authors = [{name = "LIghtJUNction"}]
requires-python = ">=3.7"
license = {text = "MIT"}

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aivfs"]