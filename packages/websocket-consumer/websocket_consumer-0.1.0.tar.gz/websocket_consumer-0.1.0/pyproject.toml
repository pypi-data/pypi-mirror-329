[project]
name = "websocket-consumer"
version = "0.1.0"
description = "Start an embedded websocket server from the root of your project."
requires-python = ">=3.12"
readme = "README.md"
dependencies = [
    "websockets>=15.0",
]


[project.scripts]
websocket_consumer = "websocket_consumer:main"
