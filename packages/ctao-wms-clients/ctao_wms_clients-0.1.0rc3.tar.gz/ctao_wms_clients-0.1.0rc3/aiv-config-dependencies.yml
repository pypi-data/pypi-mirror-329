# TODO: make nested
# Note that if sonar key is not specified, it's read from the project's sonar-project.properties
dependencies:
  - gitlab_path: cta-computing/dpps/workload/CTADIRAC
    revision: 2.2.62
