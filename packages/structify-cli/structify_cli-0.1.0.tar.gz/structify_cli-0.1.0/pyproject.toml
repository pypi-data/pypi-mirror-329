[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "structify-cli"
version = "0.1.0"
description = "A CLI tool to quickly generate project structures."
authors = [{name = "Ameer Hamza Khan", email = "your-email@example.com"}]
license = {text = "MIT"}
dependencies = []

[project.scripts]
structify = "structify.cli:main"

[tool.setuptools.packages.find]
where = ["."]
