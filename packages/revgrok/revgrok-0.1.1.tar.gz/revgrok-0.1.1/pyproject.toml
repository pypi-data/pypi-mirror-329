[project]
name = "revgrok"
version = "0.1.1"
authors = [
    {name = "linkedlist771", email = "213193509seu@gmail.com"},
]
description = "This is an unofficial implementation of the grok as api(reverse)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "build>=1.2.2.post1",
    "httpx>=0.28.1",
    "twine>=6.1.0",
]

[project.urls]
Homepage = "https://github.com/linkedlist771/revGrok"
Repository = "https://github.com/linkedlist771/revGrok.git"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
