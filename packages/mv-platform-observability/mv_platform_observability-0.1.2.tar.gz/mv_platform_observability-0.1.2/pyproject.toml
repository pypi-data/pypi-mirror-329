[project]
name = "mv-platform-observability"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ruslan_Mendybayev", email = "ruslan_mendybayev@epam.com" }
]
requires-python = ">=3.10"
dependencies = [
    "mv-platform-core",
    "azure-eventhub>=5.14.0",
    "azure-identity>=1.20.0",
    "pydantic>=2.10.6",
    "requests>=2.32.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "pytest-asyncio>=0.25.3",
    "python-dotenv>=1.0.1",
    "pyspark",
    "delta-spark",
    "findspark"
]

[tool.uv.sources]
mv-platform-core = {path ="../core", editable=true}

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = "."
