[project]
name = "irrectum"
version = "0.1.2"
description = "Metrics for Evaluating Model Quality"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26.0",
    "setuptools>=69.1.0",
]

[project.readme]
file = "README.md"
content-type = "text/markdown"

[dependency-groups]
dev = [
    "twine>=6.1.0",
]
