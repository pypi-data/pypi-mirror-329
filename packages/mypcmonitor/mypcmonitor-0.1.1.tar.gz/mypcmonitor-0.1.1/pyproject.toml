[tool.poetry]
name = "mypcmonitor"
version = "0.1.1"
description = ""
authors = ["bdizen <bdizenweb@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
psutil = "^7.0.0"
textual = "^2.1.1"

[tool.poetry.scripts]
pcmonitor-local = "mypcmonitor.local.main:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
