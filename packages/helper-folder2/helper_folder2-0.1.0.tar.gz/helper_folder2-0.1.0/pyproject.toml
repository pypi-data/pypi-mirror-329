[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "helper_folder2"
version = "0.1.0"
description = "Helper functions for plotting line and loading and training data"
authors = [{name = "Zoe Herman"}]