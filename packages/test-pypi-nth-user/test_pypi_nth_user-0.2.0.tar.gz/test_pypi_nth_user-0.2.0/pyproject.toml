[project]
name = "test-pypi-nth-user"
version = "0.2.0"
description = "this is a test to get nth user from an API"
authors = [
    {name = "365ping",email = "abc@def.com"}
]
readme = "README.md"
requires-python = ">=3.9"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.0" # any 2.x

[tool.poetry]
packages = [{include = "test_pypi_nth_user", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
